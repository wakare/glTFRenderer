#include "VKShaderTable.h"

bool VKShaderTable::InitShaderTable(IRHIDevice& device, IRHIPipelineStateObject& pso, IRHIRayTracingAS& as,
    const std::vector<RHIShaderBindingTable>& sbts)
{
    return true;
}
