#include "VKGPUBufferManager.h"

bool VKGPUBufferManager::InitGPUBufferManager(IRHIDevice& device, size_t maxDescriptorCount)
{
    return true;
}

bool VKGPUBufferManager::CreateGPUBuffer(IRHIDevice& device, const RHIBufferDesc& bufferDesc, size_t& outBufferIndex)
{
    return true;
}
