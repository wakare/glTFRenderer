#include "VKIndexBuffer.h"

std::shared_ptr<IRHIIndexBufferView> VKIndexBuffer::CreateIndexBufferView(IRHIDevice& device, IRHIMemoryManager& memory_manager,
    IRHICommandList& command_list, const RHIBufferDesc& desc, const IndexBufferData& index_buffer_data)
{
    return nullptr;
}
